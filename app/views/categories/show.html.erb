<div class="card">
  <div class="card-body">
    <h3 class="card-title"><%= @category.name %></h3>
    <p class="card-text"><%= @category.description %></p>

    <hr>
    <%= link_to "Edytuj", edit_category_path(@category), class: "btn btn-primary" %>
    <%= link_to "Powrót", categories_path, class: "btn btn-secondary" %>
  </div>
</div>
