<div class="card">
  <div class="card-body">
    <h3 class="card-title"><%= @manufacturer.name %></h3>
    <p class="card-text"><%= @manufacturer.description %></p>

    <hr>
    <%= link_to "Edytuj", edit_manufacturer_path(@manufacturer), class: "btn btn-primary" %>
    <%= link_to "Powrót", manufacturers_path, class: "btn btn-secondary" %>
  </div>
</div>
