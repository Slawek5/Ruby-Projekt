<div class="card">
  <div class="card-body">
    <h3 class="card-title"><%= @part.title %></h3>
    <h6 class="card-subtitle text-muted">SKU: <%= @part.sku %></h6>
    <hr>

    <p><strong>Status:</strong> <%= @part.status %></p>
    <p><strong>Kategoria:</strong> <%= @part.category&.name || "Brak" %></p>
    <p class="mt-3"><strong>Opis:</strong><br><%= @part.description.presence || "Brak opisu" %></p>

    <hr>
    <%= link_to "Edytuj", edit_part_path(@part), class: "btn btn-primary" %>
    <%= link_to "Powrót", parts_path, class: "btn btn-secondary" %>
  </div>
</div>
